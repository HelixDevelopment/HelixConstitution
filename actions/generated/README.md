# Generated action-prefix slash commands (§11.4.140)

Auto-generated by `scripts/generate_agent_prefix_commands.sh` from
`actions/registry.yaml`. DO NOT EDIT BY HAND — re-run the generator.

Two slash forms per action (GRAMMAR_ADDENDUM §11.4.140):

- **`<name>`** — the bare slash form 3 `/<name>`.
- **`default-<name>`** — the namespaced slash form 4 `/DEFAULT::<name>`
  alias (these agents disallow `::` in a command name). It maps to the
  IDENTICAL expansion, so `/<name>` ≡ `/default-<name>`. **Collision
  fallback:** if `/<name>` ever collides with a host built-in command on
  a given agent, invoke `/default-<name>` (the always-safe form-4
  equivalent).

Per-agent install targets (the install script wires these by reference):

| Agent | Source here | Install location (consuming project) |
|---|---|---|
| Gemini CLI | `gemini/<name>.toml`, `gemini/default-<name>.toml` | `.gemini/commands/` |
| Qwen Code  | `qwen/<name>.toml`, `qwen/default-<name>.toml`     | `.qwen/commands/` |
| Codex CLI  | `codex/<name>.md`, `codex/default-<name>.md`       | `prompts/` |

Generated 5 action(s) (each as a bare + a `default-` namespaced command).
