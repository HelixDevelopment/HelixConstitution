# Build derivative — regenerated by `go build ./cmd/scheduled-work`
# (§11.4.30 no-versioned-build-artifacts; §11.4.77 regen mechanism = build.sh / go build)
/bin/
# Runtime store + captured evidence (regenerated per run)
*.json
!go.sum
qa-results/
