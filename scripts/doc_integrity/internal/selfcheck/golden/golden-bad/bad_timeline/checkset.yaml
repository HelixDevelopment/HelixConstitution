schema_version: 1
# GOLDEN-BAD (TIMELINE): a dependent item starts before its dependency finishes
# — deadline-before-dependency. MUST FAIL TIME-02.
sources:
  - id: plan
    kind: markdown-table
    path: plan.md
    columns:
      item_id: "№"
      ticket: "Ticket"
      name_en: "Task"
      scope: "Scope"
      start: "Start"
      deadline: "Deadline"
      deps: "Deps"
